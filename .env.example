# Copy to .env.local for `vercel dev`, or set in Vercel → Project → Settings → Environment Variables

# Portal login (server-side — required for /api/* routes)
BASIC_AUTH_USER=Admin
BASIC_AUTH_PASSWORD=AfePortal123!@#

# SiteGround MySQL (remote)
MYSQL_HOST=35.209.193.155
MYSQL_PORT=3306
MYSQL_USER=u5s3nwinygnfy
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=dbnivmbmdqngwq

# SiteGround: Site Tools → Site → MySQL → Remote — whitelist % for Vercel
